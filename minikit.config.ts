import { defineConfig } from "@coinbase/onchainkit/minikit";

export default defineConfig({
  name: "Co-Mission", // Your app name
  url: "https://localhost:3000", // Change to your deployed URL later
  description: "My first Base mini app with OnchainKit",
  icon: "https://yourdomain.com/icon.png", // optional, replace with your logo
});
